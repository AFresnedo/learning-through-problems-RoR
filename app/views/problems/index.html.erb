<% provide(:title, 'Problems Index') %>
<h1>All Problems For Curriculum</h1>

<%= will_paginate %>

<ul class="problems">
  <% @problems.each do |problem| %>
    <li>
      <%= link_to problem.filename, problem %>
      <% if is_teacher %>
        | <%= link_to "edit", edit_problem_url(problem.id) %>
      <% end %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
